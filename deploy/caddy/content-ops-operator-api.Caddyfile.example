ops-api.example.com {
    encode zstd gzip

    # Optional hardening. Keep the shared-secret header required at the app layer.
    header {
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        Referrer-Policy no-referrer
    }

    reverse_proxy 127.0.0.1:8765
}
